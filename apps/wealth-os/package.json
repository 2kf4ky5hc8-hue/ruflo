{
  "name": "@ruflo/wealth-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ruflo Wealth OS — personal wealth command centre (decision-support, not regulated advice)",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/db/seed.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@claude-flow/hooks": "workspace:*",
    "@claude-flow/memory": "workspace:*",
    "@claude-flow/security": "workspace:*",
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5",
    "yaml": "^2.6.0",
    "zod": "^3.23.8"
  },
  "peerDependenciesMeta": {
    "@claude-flow/hooks": { "optional": true },
    "@claude-flow/memory": { "optional": true },
    "@claude-flow/security": { "optional": true }
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
