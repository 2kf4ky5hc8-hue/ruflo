node_modules
dist
.env
.env.local
*.local
.DS_Store
.vite

# Intentionally committed: holds PUBLIC client values only (Supabase project URL
# + publishable/anon key) so the Cloudflare build self-configures. No secrets.
# Overrides the repo-root ".env.*" ignore rule for this one file.
!.env.production
