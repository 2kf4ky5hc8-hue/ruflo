# STAGING build configuration — used only by `npm run build:staging`
# (vite build --mode staging). This points the staging Cloudflare deploy at the
# SEPARATE staging Supabase project, never at live data.
#
# PUBLIC client values only (staging project URL + publishable/anon key) — safe
# to commit; they ship to the browser anyway and data is protected by RLS.
# NEVER put the service_role key or the database password in this file.
VITE_SUPABASE_URL=https://tffpiulparxnxgicdhbr.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_1WtQWz8NWRhjd-83ea6CNQ_Ve4LAsz8
